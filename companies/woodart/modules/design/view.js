/* ============================================================================
 * AUTO-GENERATED by tools/build/build-module.mjs — DO NOT EDIT THIS FILE.
 * Sources (edit these, then rebuild):
 *   frontend/template.html   — the markup
 *   frontend/api.js         — the data seam
 *   frontend/design.js   — the logic
 *   (no module stylesheet)
 * Rebuild:  node tools/build/build-module.mjs companies/woodart/modules/design
 * ==========================================================================*/
(function () {
  'use strict';
  var TEMPLATE_HTML = "<!-- ============================================================================\n  WOODART · DESIGN & 3D · MARKUP\n  ----------------------------------------------------------------------------\n  THIS FILE IS THE SCREEN. Open it and you see the whole UI, head bar to footer.\n  No <script> tag, no <template> tag — standing owner rule\n  (companies/woodart/MODULE-STANDARD.md §2.1).\n\n  WHAT THIS MODULE IS (companies/woodart/ROOT-MAP.md §1): a project moves\n  through PHASES, and each delivery phase has ONE module owning the work\n  records produced during it. Production owns fabrication jobs, Installation\n  owns site visits — and this owns the ARCHITECTURE & 3D phase: drawings, 3D\n  models, their revisions, and the client approval that is the gate out of it.\n\n  THE HOOKS: [data-shell] chrome · [data-screen] a screen · [data-fill] a slot ·\n  [data-k] a live value · [data-tab] a tab button · [data-act] an action ·\n  [data-when] a block removed when false · [data-proto] a cloned row ·\n  [data-slot] a fill point inside a cloned row.\n\n  STYLING: every UTILITY is Tailwind (tw-); house COMPONENT classes stay.\n  The only inline styles are computed bar widths — values, not utilities.\n  ============================================================================ -->\n\n\n<!-- ================================================== SHARED CHROME ======= -->\n<div data-shell=\"head\" class=\"page-head\">\n  <div>\n    <h1 class=\"page-title\" data-fill=\"title\"><span class=\"eyebrow\" data-fill=\"eyebrow\"></span><i class=\"bi bi-vector-pen\"></i></h1>\n    <p class=\"page-sub\" data-fill=\"sub\"></p>\n  </div>\n  <div class=\"page-actions\">\n    <a class=\"btn btn-ghost\" href=\"#/woodart/projects/active\"><i class=\"bi bi-easel2-fill\"></i> Projects</a>\n    <button class=\"btn btn-primary\" data-act=\"new\"><i class=\"bi bi-plus-lg\"></i> New Drawing</button>\n  </div>\n</div>\n\n<div data-shell=\"tabs\" class=\"tab-underline mb-3\">\n  <button data-tab=\"register\">Drawing Register</button>\n  <button data-tab=\"approvals\">Approvals</button>\n  <button data-tab=\"load\">Design Load</button>\n</div>\n\n\n<!-- ============================================================================\n     SCREEN 1 · DRAWING REGISTER — every deliverable of the design phase.\n     ============================================================================ -->\n<section data-screen=\"register\">\n\n  <div class=\"kpi-grid kpi-compact stagger\">\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Deliverables</span><span class=\"kpi-ico\"><i class=\"bi bi-vector-pen\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"drawings\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">With Client</span><span class=\"kpi-ico\"><i class=\"bi bi-send\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"issued\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Needs Revising</span><span class=\"kpi-ico\"><i class=\"bi bi-arrow-counterclockwise\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"commented\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Approved</span><span class=\"kpi-ico\"><i class=\"bi bi-patch-check-fill\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"approved\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Designers</span><span class=\"kpi-ico\"><i class=\"bi bi-people-fill\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"designers\">—</div>\n    </div>\n  </div>\n\n  <!-- Shown only while something is waiting on the client or needs revising. -->\n  <div class=\"build-banner\" data-when=\"attention\">\n    <i class=\"bi bi-hourglass-split\"></i>\n    <div><strong data-k=\"attentionInline\">0</strong> deliverable(s) are sitting with the client\n      or waiting to be revised — the design phase cannot close until they are approved.\n      <button class=\"link-btn\" data-act=\"goApprovals\">Open the approval queue</button>.</div>\n  </div>\n\n  <div class=\"card\">\n    <div class=\"card-head\">\n      <h3><i class=\"bi bi-collection\"></i> Drawing &amp; Model Register</h3>\n      <span class=\"card-sub\">Plans, elevations, sections, details, 3D models and renders — with their current revision</span>\n    </div>\n    <div class=\"card-body\" data-fill=\"register\"></div>\n  </div>\n\n</section>\n\n\n<!-- ============================================================================\n     SCREEN 2 · APPROVALS — the gate out of the design phase.\n     Two mutually exclusive states, BOTH authored here.\n     ============================================================================ -->\n<section data-screen=\"approvals\">\n\n  <div class=\"kpi-grid kpi-compact stagger\">\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Awaiting Client</span><span class=\"kpi-ico\"><i class=\"bi bi-hourglass-split\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"waiting\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Longest Wait</span><span class=\"kpi-ico\"><i class=\"bi bi-alarm-fill\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"oldest\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Projects Design-Complete</span><span class=\"kpi-ico\"><i class=\"bi bi-patch-check-fill\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"complete\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Avg Revisions</span><span class=\"kpi-ico\"><i class=\"bi bi-arrow-repeat\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"avgRev\">—</div>\n    </div>\n  </div>\n\n  <!-- STATE A · nothing outstanding -->\n  <div class=\"empty-state\" data-when=\"clear\">\n    <i class=\"bi bi-check2-circle\"></i>\n    <h3>Nothing waiting on the client</h3>\n    <p class=\"tw-text-ink-dim\">Every issued drawing has been approved. No project is blocked by design.</p>\n  </div>\n\n  <!-- STATE B · there is a queue -->\n  <div data-when=\"some\">\n    <div class=\"build-banner\">\n      <i class=\"bi bi-info-circle\"></i>\n      <div>A project's design phase is complete only when every one of its deliverables is\n        <strong>Approved</strong>. Issued work is with the client; commented work is back\n        with the designer.</div>\n    </div>\n    <div class=\"card\">\n      <div class=\"card-head\">\n        <h3><i class=\"bi bi-hourglass-split\"></i> Approval Queue</h3>\n        <span class=\"card-sub\">Longest wait first — this is what is holding the phase open</span>\n      </div>\n      <div class=\"card-body\" data-fill=\"register\"></div>\n    </div>\n  </div>\n\n</section>\n\n\n<!-- ============================================================================\n     SCREEN 3 · DESIGN LOAD — who is carrying the design work.\n     ============================================================================ -->\n<section data-screen=\"load\">\n\n  <div class=\"kpi-grid kpi-compact stagger\">\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Designers</span><span class=\"kpi-ico\"><i class=\"bi bi-people-fill\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"designers\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Busiest</span><span class=\"kpi-ico\"><i class=\"bi bi-fire\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"top\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Open Deliverables</span><span class=\"kpi-ico\"><i class=\"bi bi-vector-pen\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"open\">—</div>\n    </div>\n    <div class=\"kpi-card\">\n      <div class=\"kpi-top\"><span class=\"kpi-label\">Approval Rate</span><span class=\"kpi-ico\"><i class=\"bi bi-percent\"></i></span></div>\n      <div class=\"kpi-value\" data-k=\"rate\">—</div>\n    </div>\n  </div>\n\n  <!-- STATE A · nothing drawn yet -->\n  <div class=\"empty-state\" data-when=\"none\">\n    <i class=\"bi bi-vector-pen\"></i>\n    <h3>No design work yet</h3>\n    <p class=\"tw-text-ink-dim\">Add a drawing or a 3D model and the load analysis fills in.</p>\n  </div>\n\n  <!-- STATE B · there is work to analyse -->\n  <div data-when=\"some\">\n\n    <div class=\"two-col\">\n\n      <div class=\"card\">\n        <div class=\"card-head\">\n          <h3><i class=\"bi bi-bar-chart-fill\"></i> Open Work by Designer</h3>\n          <span class=\"card-sub\">What each designer still has to get approved</span>\n        </div>\n        <div class=\"card-body\">\n          <div class=\"data-list\" data-fill=\"designers\">\n            <!-- PROTOTYPE ROW — one per designer; the count is DATA, not layout. -->\n            <div class=\"data-row\" data-proto=\"designer\" hidden>\n              <div class=\"tw-flex-1\">\n                <div class=\"tw-font-semibold\" data-slot=\"name\"></div>\n                <div class=\"progress tw-mt-[6px]\"><div class=\"progress-bar\" data-slot=\"bar\"></div></div>\n              </div>\n              <div class=\"tw-ml-auto tw-text-right\">\n                <div class=\"num tw-font-semibold\" data-slot=\"open\"></div>\n                <div class=\"tw-text-ink-mute tw-text-[11px]\" data-slot=\"detail\"></div>\n              </div>\n            </div>\n          </div>\n        </div>\n      </div>\n\n      <div class=\"card\">\n        <div class=\"card-head\">\n          <h3><i class=\"bi bi-pie-chart-fill\"></i> Deliverable Mix</h3>\n          <span class=\"card-sub\">By kind — drawings vs 3D work</span>\n        </div>\n        <div class=\"card-body\">\n          <div class=\"tw-relative tw-h-[260px]\"><canvas data-fill=\"chart\"></canvas></div>\n        </div>\n      </div>\n\n    </div>\n\n    <div class=\"card\">\n      <div class=\"card-head\">\n        <h3><i class=\"bi bi-table\"></i> Designer Breakdown</h3>\n        <span class=\"card-sub\">Deliverables, open work, revisions carried and approvals</span>\n      </div>\n      <div class=\"card-body\" data-fill=\"register\"></div>\n    </div>\n\n  </div>\n\n</section>\n";
  var MODULE_CSS = null;
  if (MODULE_CSS && !document.querySelector('style[data-module-style="woodart/design"]')) {
    var st = document.createElement('style');
    st.setAttribute('data-module-style', 'woodart/design');
    st.textContent = MODULE_CSS;
    document.head.appendChild(st);
  }
  /* ---- frontend/api.js — the data seam ------------------------------ */
/* ============================================================================
 * WOODART · DESIGN & 3D · DATA SEAM
 * ----------------------------------------------------------------------------
 * THE ONLY FILE IN THIS MODULE THAT KNOWS WHERE DESIGN DATA COMES FROM.
 *
 * The screen calls `Design.register()` / `Design.queue()`. It does not know
 * whether that resolves to localStorage (demo mode) or to
 * GET /api/woodart/design/... (API mode).
 *
 * WHY THIS MODULE EXISTS (companies/woodart/ROOT-MAP.md §1)
 * ---------------------------------------------------------------------------
 * A project moves through PHASES, and each delivery phase has ONE module that
 * owns the work records produced during it. Production owns fabrication jobs;
 * Installation owns site visits. The ARCHITECTURE & 3D phase had no owner at
 * all — its work lived in a kanban column called "Design Studio" and nowhere
 * else. This module is that owner.
 *
 * TWO STORES, ONE LIFECYCLE
 * ---------------------------------------------------------------------------
 *   wa_drawings   the deliverable, carrying its CURRENT revision and status
 *   wa_revisions  the trail — one row per revision letter, per action
 *
 * The trail is a separate store, not a JSON blob on the drawing, because a
 * revision is an AUDIT record: who issued it, what the client said, when it was
 * approved. Installation embeds its snag list because a snag is a checklist
 * item; a revision is evidence, and evidence gets its own row.
 *
 * THE RECORD SHAPES (seeded by platform/data/seed-bd.js; the exact shapes the
 * Laravel Resources return — see backend/endpoints.md):
 *   drawing  { id:'DWG-001', project:'WAP-001', title:'Plan — Office Fit-out',
 *              kind:'Plan', rev:'B', status:'Issued', designer:'Nasrin Sultana',
 *              issued:'2026-06-14', approved:null, created:'2026-06-01' }
 *   revision { id:'RVN-001', drawing:'DWG-001', rev:'A', action:'Revised',
 *              by:'Nasrin Sultana', note:'Client comments incorporated',
 *              date:'2026-06-02' }
 * ==========================================================================*/

/* Emitted FIRST inside the module IIFE (build-module.mjs), so this file owns the
 * shared bindings; frontend/design.js adds only its view-layer ones. */
var EPAL = window.EPAL, db = EPAL.db;

var DRAWINGS = 'wa_drawings';    /* ← the one place these are named */
var REVISIONS = 'wa_revisions';
var PROJECTS = 'wa_projects';    /* read-only: this module never writes projects */

var TODAY = '2026-07-05';        /* the demo clock — same anchor as every module */

var KINDS = ['Plan', 'Elevation', 'Section', 'Detail', '3D Model', 'Render'];
var STATUSES = ['Draft', 'Issued', 'Commented', 'Approved'];
var ACTIONS = ['Drafted', 'Issued', 'Commented', 'Revised', 'Approved'];

var Design = {

  kinds: function () { return KINDS.slice(); },
  statuses: function () { return STATUSES.slice(); },
  today: function () { return TODAY; },

  /** THE open rule: anything not Approved is still design work in flight. */
  isOpen: function (d) { return d.status !== 'Approved'; },

  /** Sitting with the CLIENT — issued and not yet answered. This is the only
   *  state where the wait is somebody else's; everything else is on us. */
  isWaiting: function (d) { return d.status === 'Issued'; },

  /** Days a drawing has been sitting with the client. NaN when not issued. */
  waitingDays: function (d) {
    if (!Design.isWaiting(d) || !d.issued) return NaN;
    var a = new Date(d.issued).getTime(), b = new Date(TODAY).getTime();
    if (isNaN(a)) return NaN;
    return Math.max(0, Math.round((b - a) / 86400000));
  },

  /** Every deliverable, most recently issued first, undated last. */
  register: function () {
    return db.col(DRAWINGS).slice().sort(function (a, b) {
      var x = a.issued || '', y = b.issued || '';
      if (x === y) return String(a.id).localeCompare(String(b.id));
      if (!x) return 1;
      if (!y) return -1;
      return x < y ? 1 : -1;
    });
  },

  find: function (id) {
    return db.col(DRAWINGS).filter(function (d) { return d.id === id; })[0] || null;
  },

  /** The full revision trail of one deliverable, oldest first. */
  trail: function (drawingId) {
    return db.col(REVISIONS).filter(function (r) { return r.drawing === drawingId; })
      .sort(function (a, b) { return String(a.rev).localeCompare(String(b.rev)); });
  },

  /** How many times a deliverable has been revised (A = none). */
  revCount: function (d) {
    var n = String(d.rev || 'A').charCodeAt(0) - 65;
    return n > 0 ? n : 0;
  },

  /** The next revision letter after the current one. */
  nextRev: function (d) {
    return String.fromCharCode(Math.min(90, String(d.rev || 'A').charCodeAt(0) + 1));
  },

  /** Only what is with the client, longest wait first — the approval queue. */
  queue: function () {
    return Design.register().filter(Design.isWaiting)
      .map(function (d) { return { rec: d, days: Design.waitingDays(d) }; })
      .sort(function (a, b) { return (b.days || 0) - (a.days || 0); });
  },

  /** The project behind a deliverable, or null when it points at a deleted one.
   *  Kept and flagged, never hidden — an orphan is a data problem to SEE. */
  projectOf: function (d) {
    return db.col(PROJECTS).filter(function (p) { return p.id === d.project; })[0] || null;
  },

  projectOptions: function () {
    return db.col(PROJECTS).slice()
      .sort(function (a, b) { return String(a.id).localeCompare(String(b.id)); })
      .map(function (p) { return [p.id, p.id + ' · ' + (p.name || '')]; });
  },

  designerOptions: function () {
    var seen = {};
    (db.employees ? db.employees({ companyId: 'woodart' }) : []).forEach(function (e) { seen[e.name] = 1; });
    db.col(DRAWINGS).forEach(function (d) { if (d.designer) seen[d.designer] = 1; });
    return Object.keys(seen).sort();
  },

  /** THE PHASE GATE, and the reason this module matters to the project spine:
   *  a project's design phase is complete only when it HAS deliverables and
   *  every one of them is Approved. A project with none has not started design
   *  — which is NOT the same as being finished, and must never count as such. */
  projectStatus: function () {
    var acc = {};
    db.col(DRAWINGS).forEach(function (d) {
      var k = d.project || 'Unassigned';
      if (!acc[k]) acc[k] = { project: k, total: 0, open: 0, waiting: 0, approved: 0 };
      acc[k].total += 1;
      if (Design.isOpen(d)) acc[k].open += 1; else acc[k].approved += 1;
      if (Design.isWaiting(d)) acc[k].waiting += 1;
    });
    return Object.keys(acc).map(function (k) {
      var r = acc[k];
      r.complete = r.total > 0 && r.open === 0;
      return r;
    }).sort(function (a, b) { return b.open - a.open || b.total - a.total; });
  },

  /** Load per designer, busiest (most OPEN deliverables) first. */
  byDesigner: function () {
    var acc = {};
    db.col(DRAWINGS).forEach(function (d) {
      var k = d.designer || 'Unassigned';
      if (!acc[k]) acc[k] = { name: k, total: 0, open: 0, waiting: 0, revisions: 0, approved: 0 };
      acc[k].total += 1;
      acc[k].revisions += Design.revCount(d);
      if (Design.isOpen(d)) acc[k].open += 1; else acc[k].approved += 1;
      if (Design.isWaiting(d)) acc[k].waiting += 1;
    });
    return Object.keys(acc).map(function (k) { return acc[k]; })
      .sort(function (a, b) { return b.open - a.open || b.total - a.total; });
  },

  /** Deliverables grouped by kind — the mix chart. */
  byKind: function () {
    var acc = {};
    db.col(DRAWINGS).forEach(function (d) {
      var k = d.kind || 'Other';
      if (!acc[k]) acc[k] = { name: k, count: 0 };
      acc[k].count += 1;
    });
    return Object.keys(acc).map(function (k) { return acc[k]; })
      .sort(function (a, b) { return b.count - a.count; });
  },

  /** The header figures every screen quotes. One calculation, one truth. */
  summary: function () {
    var all = db.col(DRAWINGS);
    var issued = 0, commented = 0, approved = 0, draft = 0, revs = 0, designers = {};
    all.forEach(function (d) {
      if (d.status === 'Issued') issued += 1;
      if (d.status === 'Commented') commented += 1;
      if (d.status === 'Draft') draft += 1;
      if (!Design.isOpen(d)) approved += 1;
      revs += Design.revCount(d);
      if (d.designer) designers[d.designer] = 1;
    });
    var q = Design.queue();
    var projects = Design.projectStatus();
    var load = Design.byDesigner();
    return {
      drawings: all.length, issued: issued, commented: commented,
      approved: approved, draft: draft,
      open: all.length - approved,
      attention: issued + commented,
      waiting: q.length,
      oldest: q.length && !isNaN(q[0].days) ? q[0].days + 'd' : '—',
      complete: projects.filter(function (p) { return p.complete; }).length,
      projects: projects.length,
      avgRev: all.length ? (revs / all.length).toFixed(1) : '0.0',
      designers: Object.keys(designers).length,
      rate: all.length ? Math.round(approved / all.length * 100) : 0,
      top: load.length && load[0].open ? load[0].name : '—'
    };
  },

  nextId: function () { return nextSeq(DRAWINGS, 'DWG'); },
  nextRevId: function () { return nextSeq(REVISIONS, 'RVN'); },

  /** Save a deliverable AND record what happened to it in the trail.
   *  A status change is evidence, so it is never saved without a row saying
   *  who did it and when — the same principle as a ledger reversal. */
  save: function (rec, opts) {
    opts = opts || {};
    var before = Design.find(rec.id);
    var saved = db.save(DRAWINGS, rec) || rec;
    var changed = !before || before.status !== saved.status || before.rev !== saved.rev;
    if (changed) {
      db.save(REVISIONS, {
        id: Design.nextRevId(), drawing: saved.id, rev: saved.rev || 'A',
        action: actionFor(before, saved), by: saved.designer || '',
        note: opts.note || '', date: TODAY
      });
    }
    return saved;
  },

  /** Delete a deliverable and its trail — the trail has no meaning without it. */
  remove: function (id) {
    db.col(REVISIONS).filter(function (r) { return r.drawing === id; })
      .forEach(function (r) { db.remove(REVISIONS, r.id); });
    return db.remove(DRAWINGS, id);
  }
};

/** What to call the transition that just happened. Named rather than inlined so
 *  the trail's vocabulary lives in one place (NAMING-AND-TERMINOLOGY §1.2). */
function actionFor(before, after) {
  if (!before) return after.status === 'Draft' ? 'Drafted' : after.status;
  if (before.rev !== after.rev) return 'Revised';
  return ACTIONS.indexOf(after.status) >= 0 ? after.status : 'Revised';
}

/** Next free id in a seeded PREFIX-000 series. */
function nextSeq(store, prefix) {
  var max = 0, re = new RegExp('^' + prefix + '-?');
  db.col(store).forEach(function (r) {
    var n = parseInt(String(r.id || '').replace(re, ''), 10);
    if (!isNaN(n) && n > max) max = n;
  });
  return prefix + '-' + String(max + 1).replace(/^(\d)$/, '00$1').replace(/^(\d\d)$/, '0$1');
}

  /* ---- frontend/design.js ---------------------------------------- */
/* ============================================================================
 * WOODART · DESIGN & 3D · LOGIC
 * ----------------------------------------------------------------------------
 * BEHAVIOUR ONLY. Every container, card, KPI tile, banner and bar is real HTML
 * in frontend/template.html, handed to this file by tools/build/build-module.mjs
 * as TEMPLATE_HTML. This file is NOT an IIFE — the build wraps it.
 *
 * WHERE THE DATA COMES FROM: frontend/api.js — the seam. This file never names
 * `wa_drawings` / `wa_revisions` and never names a URL. Grep it: neither is here.
 *
 * ==> LARAVEL: register = GET|POST /api/woodart/design/drawings,
 *     queue = GET .../approvals, load = GET .../load. See backend/endpoints.md.
 * ==========================================================================*/

var ui = EPAL.ui, el = ui.el;

var CID = 'woodart';
var ROUTE = 'woodart/design';

/* ---- template plumbing --------------------------------------------------- */
var TPL = document.createElement('div');
TPL.innerHTML = TEMPLATE_HTML;

function screen(name) { return TPL.querySelector('[data-screen="' + name + '"]').cloneNode(true); }
function shell(name) { return TPL.querySelector('[data-shell="' + name + '"]').cloneNode(true); }
function fill(root, k) { return root.querySelector('[data-fill="' + k + '"]'); }
function fillK(root, k, v) { var n = root.querySelector('[data-k="' + k + '"]'); if (n) n.textContent = String(v); }
function slot(root, k) { return root.querySelector('[data-slot="' + k + '"]'); }
function mountScreen(page, s) { Array.prototype.slice.call(s.children).forEach(function (c) { page.appendChild(c); }); }

function when(root, name, keep) {
  var n = root.querySelector('[data-when="' + name + '"]');
  if (!n) return null;
  if (!keep) { n.parentNode.removeChild(n); return null; }
  n.removeAttribute('data-when');
  return n;
}
function proto(root, name) {
  var n = root.querySelector('[data-proto="' + name + '"]').cloneNode(true);
  n.removeAttribute('data-proto'); n.removeAttribute('hidden');
  return n;
}
function dropProtos(root) {
  Array.prototype.forEach.call(root.querySelectorAll('[data-proto]'), function (p) { p.parentNode.removeChild(p); });
}

/* ---- shared chrome ------------------------------------------------------- */

var TAB_COPY = {
  register:  ['Drawing Register', 'Plans, elevations, sections, details, 3D models and renders — with their current revision.'],
  approvals: ['Approvals', 'What is sitting with the client. A project’s design phase closes only when all of it is approved.'],
  load:      ['Design Load', 'Who is carrying the design work, and how much of it is still open.']
};

function head(sub) {
  var copy = TAB_COPY[sub] || TAB_COPY.register;
  var h = shell('head');
  fill(h, 'eyebrow').textContent = sub === 'register' ? 'Woodart Interiors' : 'Woodart › Design & 3D';
  fill(h, 'title').appendChild(document.createTextNode(copy[0]));
  var s = fill(h, 'sub');
  s.textContent = copy[1];
  s.setAttribute('title', copy[1]);
  var add = h.querySelector('[data-act="new"]');
  if (!canCreate()) add.parentNode.removeChild(add);
  else add.addEventListener('click', function () { editDrawing(null); });
  return h;
}

function tabs(sub) {
  var band = shell('tabs');
  Array.prototype.forEach.call(band.querySelectorAll('[data-tab]'), function (btn) {
    var key = btn.getAttribute('data-tab');
    if (key === sub) btn.classList.add('active');
    btn.removeAttribute('data-tab');
    btn.addEventListener('click', function () {
      EPAL.router.navigate(ROUTE + (key === 'register' ? '' : '/' + key));
    });
  });
  return band;
}

/* ---- permissions + formatters -------------------------------------------- */
function canCreate() { return !EPAL.perm || EPAL.perm.can(CID, 'design', 'create'); }
function canDelete() { return !EPAL.perm || EPAL.perm.can(CID, 'design', 'delete'); }

var STATUS_TONE = { Approved: 'good', Issued: '', Commented: 'bad', Draft: 'warn' };

function projectCell(r) {
  var p = Design.projectOf(r);
  if (p) return '<span class="strong">' + ui.escapeHtml(r.project) + '</span> <span class="text-mute xs">' + ui.escapeHtml(p.name || '') + '</span>';
  return ui.escapeHtml(r.project || '—') + ' <span class="badge badge-warn">orphan</span>';
}
function revCell(r) {
  var n = Design.revCount(r);
  return '<span class="badge">Rev ' + ui.escapeHtml(r.rev || 'A') + '</span>' +
    (n ? ' <span class="text-mute xs">' + n + ' revision' + (n === 1 ? '' : 's') + '</span>' : '');
}
function waitCell(r) {
  var d = Design.waitingDays(r);
  if (isNaN(d)) return '<span class="text-mute">—</span>';
  var tone = d > 14 ? 'text-bad' : d > 7 ? 'text-warn' : '';
  return '<span class="num ' + tone + '">' + d + 'd</span>';
}

/* ============================================================================
 * THE VIEW
 * ==========================================================================*/
EPAL.view(ROUTE, {
  title: function () { return 'Design & 3D'; },
  render: function (ctx) {
    var sub = ctx.subId || 'register';
    if (!TAB_COPY[sub]) sub = 'register';

    var page = el('div.page');
    page.appendChild(head(sub));
    page.appendChild(tabs(sub));

    ({ register: registerScreen, approvals: approvalsScreen, load: loadScreen }[sub])(page);

    ctx.mount.appendChild(page);
  }
});

/* ========================================================== SCREEN · REGISTER */
function registerScreen(page) {
  var s = screen('register');
  var sum = Design.summary();

  fillK(s, 'drawings', ui.num(sum.drawings));
  fillK(s, 'issued', ui.num(sum.issued));
  fillK(s, 'commented', ui.num(sum.commented));
  fillK(s, 'approved', ui.num(sum.approved));
  fillK(s, 'designers', ui.num(sum.designers));

  var banner = when(s, 'attention', sum.attention > 0);
  if (banner) {
    fillK(banner, 'attentionInline', ui.num(sum.attention));
    banner.querySelector('[data-act="goApprovals"]')
      .addEventListener('click', function () { EPAL.router.navigate(ROUTE + '/approvals'); });
  }

  fill(s, 'register').appendChild(EPAL.table({
    columns: [
      { key: 'id', label: 'Ref', render: function (r) { return '<span class="strong">' + ui.escapeHtml(r.id) + '</span>'; } },
      { key: 'title', label: 'Deliverable' },
      { key: 'kind', label: 'Kind', render: function (r) { return '<span class="badge">' + ui.escapeHtml(r.kind || '—') + '</span>'; } },
      { key: 'project', label: 'Project', render: projectCell },
      { key: 'designer', label: 'Designer' },
      { key: 'rev', label: 'Revision', render: revCell },
      { key: 'status', label: 'Status', badge: STATUS_TONE },
      { key: 'wait', label: 'With Client', num: true,
        sortVal: function (r) { var d = Design.waitingDays(r); return isNaN(d) ? -1 : d; },
        render: waitCell }
    ],
    rows: Design.register(),
    searchKeys: ['id', 'title', 'kind', 'project', 'designer'],
    filters: [{ key: 'status', label: 'Status' }, { key: 'kind', label: 'Kind' }],
    onRow: function (r) { drawingDrawer(r.id); },
    actions: canDelete() ? [{ icon: 'trash', title: 'Delete deliverable', onClick: deleteDrawing }] : null,
    exportName: 'woodart-drawings.csv',
    pageSize: 12,
    empty: { icon: 'vector-pen', title: 'No design work yet', hint: 'Add the first drawing or 3D model.' }
  }).el);

  mountScreen(page, s);
}

/* ========================================================= SCREEN · APPROVALS */
function approvalsScreen(page) {
  var s = screen('approvals');
  var sum = Design.summary();
  var q = Design.queue();

  fillK(s, 'waiting', ui.num(sum.waiting));
  fillK(s, 'oldest', sum.oldest);
  fillK(s, 'complete', sum.complete + ' / ' + sum.projects);
  fillK(s, 'avgRev', sum.avgRev);

  when(s, 'clear', q.length === 0);
  var body = when(s, 'some', q.length > 0);

  if (body) {
    var rows = q.map(function (r) {
      return { id: r.rec.id, title: r.rec.title, kind: r.rec.kind, project: r.rec.project,
        designer: r.rec.designer, rev: r.rec.rev, issued: r.rec.issued, days: r.days };
    });
    fill(body, 'register').appendChild(EPAL.table({
      columns: [
        { key: 'id', label: 'Ref', render: function (r) { return '<span class="strong">' + ui.escapeHtml(r.id) + '</span>'; } },
        { key: 'title', label: 'Deliverable' },
        { key: 'project', label: 'Project', render: projectCell },
        { key: 'designer', label: 'Designer' },
        { key: 'rev', label: 'Rev', render: function (r) { return '<span class="badge">' + ui.escapeHtml(r.rev || 'A') + '</span>'; } },
        { key: 'issued', label: 'Issued', date: true },
        { key: 'days', label: 'Waiting', num: true,
          render: function (r) {
            var tone = r.days > 14 ? 'text-bad' : r.days > 7 ? 'text-warn' : '';
            return '<span class="num strong ' + tone + '">' + ui.num(r.days) + 'd</span>';
          } }
      ],
      rows: rows,
      searchKeys: ['id', 'title', 'project', 'designer'],
      onRow: function (r) { drawingDrawer(r.id); },
      exportName: 'woodart-design-approvals.csv',
      pageSize: 12,
      empty: { icon: 'check2-circle', title: 'Nothing waiting', hint: 'Everything issued has been approved.' }
    }).el);
  }

  mountScreen(page, s);
}

/* ============================================================== SCREEN · LOAD */
function loadScreen(page) {
  var s = screen('load');
  var sum = Design.summary();
  var load = Design.byDesigner();
  var kinds = Design.byKind();

  fillK(s, 'designers', ui.num(sum.designers));
  fillK(s, 'top', sum.top);
  fillK(s, 'open', ui.num(sum.open));
  fillK(s, 'rate', sum.rate + '%');

  when(s, 'none', sum.drawings === 0);
  var body = when(s, 'some', sum.drawings > 0);

  if (body) {
    var host = fill(body, 'designers');
    var max = load.length ? load[0].open : 0;
    load.forEach(function (c) {
      var row = proto(body, 'designer');
      slot(row, 'name').textContent = c.name;
      slot(row, 'open').textContent = ui.num(c.open);
      slot(row, 'detail').textContent = c.total + ' total' +
        (c.waiting ? ' · ' + c.waiting + ' with client' : '') +
        (c.revisions ? ' · ' + c.revisions + ' revision' + (c.revisions === 1 ? '' : 's') : '');
      slot(row, 'bar').style.width = (max ? Math.round(c.open / max * 100) : 0) + '%';
      host.appendChild(row);
    });
    dropProtos(body);

    fill(body, 'register').appendChild(EPAL.table({
      columns: [
        { key: 'name', label: 'Designer', render: function (r) { return '<span class="strong">' + ui.escapeHtml(r.name) + '</span>'; } },
        { key: 'total', label: 'Deliverables', num: true },
        { key: 'open', label: 'Open', num: true },
        { key: 'waiting', label: 'With Client', num: true },
        { key: 'revisions', label: 'Revisions', num: true,
          render: function (r) {
            if (!r.revisions) return '<span class="text-mute">—</span>';
            return '<span class="num text-warn">' + ui.num(r.revisions) + '</span>';
          } },
        { key: 'approved', label: 'Approved', num: true }
      ],
      rows: load,
      searchKeys: ['name'],
      exportName: 'woodart-design-load.csv',
      pageSize: 10,
      empty: { icon: 'people', title: 'No designers assigned', hint: 'Assign a designer to a deliverable.' }
    }).el);

    var canvas = fill(body, 'chart');
    mountScreen(page, s);

    requestAnimationFrame(function () {
      if (!EPAL.charts || !kinds.length || !canvas.isConnected) return;
      EPAL.charts.doughnut(canvas, {
        labels: kinds.map(function (c) { return c.name; }),
        data: kinds.map(function (c) { return c.count; })
      });
    });
    return;
  }

  mountScreen(page, s);
}

/* ============================================== THE DRAWER (revision trail) ==
 * A per-record detail view whose row count is data — the sanctioned place for
 * JS to build DOM (UI-CONTRACT §4.2). It shows the audit trail, which is the
 * whole reason revisions are their own store rather than a blob.
 * ==========================================================================*/
function drawingDrawer(id) {
  var d = Design.find(id);
  if (!d) return;
  var body = el('div');
  var m = ui.modal({ title: d.id + ' · ' + (d.title || ''), icon: 'vector-pen', size: 'lg', body: body, footer: false });

  function redraw() {
    var cur = Design.find(id);
    if (!cur) { m.close(); return; }
    body.innerHTML = '';
    var p = Design.projectOf(cur);

    body.appendChild(el('div.flex.gap-1.flex-wrap.items-center.mb-3', null, [
      el('span.badge', { text: cur.kind || '—' }),
      el('span.badge', { text: 'Rev ' + (cur.rev || 'A') }),
      el('span.badge' + (STATUS_TONE[cur.status] ? '.badge-' + STATUS_TONE[cur.status] : ''), { text: cur.status || '—' })
    ]));

    body.appendChild(el('div.form-grid', null, [
      kv('Project', p ? cur.project + ' · ' + p.name : (cur.project || '—') + ' (orphan)'),
      kv('Designer', cur.designer || '—'),
      kv('Issued', cur.issued ? ui.date(cur.issued) : '—'),
      kv('Approved', cur.approved ? ui.date(cur.approved) : '—')
    ]));

    body.appendChild(el('div.section-label', { text: 'Revision trail' }));
    var trail = Design.trail(id);
    if (!trail.length) {
      body.appendChild(el('p.text-mute.sm', { text: 'No revisions recorded yet.' }));
    } else {
      var list = el('div.data-list');
      trail.forEach(function (r) {
        list.appendChild(el('div.data-row', null, [
          el('span.badge', { text: 'Rev ' + r.rev }),
          el('div.flex-1', null, [
            el('div.fw-600', { text: r.action }),
            el('div.text-mute.xs', { text: (r.by || '—') + (r.note ? ' · ' + r.note : '') })
          ]),
          el('span.text-mute.xs', { text: r.date ? ui.date(r.date) : '' })
        ]));
      });
      body.appendChild(list);
    }

    // The lifecycle buttons — each one records a row in the trail.
    body.appendChild(el('div.divider'));
    var acts = el('div.flex.gap-1.flex-wrap');
    if (canCreate()) {
      if (cur.status === 'Draft') acts.appendChild(btn('Issue to client', 'send', function () { move(cur, 'Issued'); }));
      if (cur.status === 'Issued') {
        acts.appendChild(btn('Client commented', 'chat-left-text', function () { move(cur, 'Commented'); }));
        acts.appendChild(btn('Approve', 'patch-check-fill', function () { move(cur, 'Approved'); }, true));
      }
      if (cur.status === 'Commented') acts.appendChild(btn('Issue revision ' + Design.nextRev(cur), 'arrow-counterclockwise', function () { revise(cur); }));
      acts.appendChild(btn('Edit', 'pencil', function () { m.close(); editDrawing(cur); }));
    }
    body.appendChild(acts);
  }

  function btn(label, icon, onclick, primary) {
    return el('button.btn.btn-sm' + (primary ? '.btn-primary' : '.btn-outline'),
      { html: ui.icon(icon) + ' ' + ui.escapeHtml(label), onclick: onclick });
  }
  function kv(k, v) {
    return el('div', null, [ el('div.text-mute.xs', { text: k }), el('div.fw-600', { text: String(v) }) ]);
  }

  /** A status move. The seam writes the trail row — this only says what and why. */
  function move(cur, status) {
    var rec = Object.assign({}, cur, { status: status });
    if (status === 'Issued' && !rec.issued) rec.issued = Design.today();
    if (status === 'Approved') rec.approved = Design.today();
    Design.save(rec);
    ui.toast(cur.id + ' → ' + status, 'success');
    redraw();
    EPAL.router.render();
  }

  /** A revision bumps the letter and puts it back with the client. */
  function revise(cur) {
    var rec = Object.assign({}, cur, {
      rev: Design.nextRev(cur), status: 'Issued', issued: Design.today(), approved: null
    });
    Design.save(rec, { note: 'Client comments incorporated' });
    ui.toast(cur.id + ' issued as Rev ' + rec.rev, 'success');
    redraw();
    EPAL.router.render();
  }

  redraw();
}

/* ============================================================ ADD / EDIT ==== */
function editDrawing(rec) {
  var isNew = !rec;
  if (isNew && !canCreate()) { ui.toast('You do not have permission to add design work', 'error'); return; }

  EPAL.formModal({
    title: isNew ? 'New Deliverable' : 'Edit · ' + rec.id,
    icon: 'vector-pen',
    size: 'md',
    record: rec || { id: Design.nextId(), kind: 'Plan', status: 'Draft', rev: 'A' },
    fields: [
      { key: 'id', label: 'Ref', type: 'text', required: true, readonly: !isNew, col2: true,
        hint: isNew ? 'Auto-numbered in the DWG-000 series.' : 'The ref is the record key and cannot change.' },
      { key: 'kind', label: 'Kind', type: 'select', required: true, col2: true, options: Design.kinds() },
      { key: 'title', label: 'Title', type: 'text', required: true, placeholder: 'e.g. Ground floor plan' },
      { key: 'project', label: 'Project', type: 'select', required: true, searchable: true,
        options: Design.projectOptions(),
        hint: 'A deliverable pointing at a deleted project is flagged "orphan" in the register.' },
      { key: 'designer', label: 'Designer', type: 'select', col2: true, searchable: true,
        options: Design.designerOptions() },
      { key: 'rev', label: 'Revision', type: 'text', col2: true,
        hint: 'A single letter. Use the drawer’s "Issue revision" button rather than editing this by hand.' },
      { key: 'status', label: 'Status', type: 'select', required: true, col2: true, options: Design.statuses(),
        hint: 'Draft → Issued → Commented → Approved. Approved closes it.' },
      { key: 'issued', label: 'Issued', type: 'date', col2: true }
    ],
    saveLabel: isNew ? 'Add Deliverable' : 'Save Changes',
    onSave: function (v) {
      if (v.status === 'Approved' && !v.approved) v.approved = Design.today();
      Design.save(v);
      ui.toast(isNew ? v.id + ' added' : v.id + ' updated', 'success');
      EPAL.router.render();
      return true;
    }
  });
}

function deleteDrawing(row) {
  var n = Design.trail(row.id).length;
  ui.confirm({
    title: 'Delete ' + row.id + '?',
    body: (row.title || 'This deliverable') + ' will be removed, along with its ' + n +
      ' revision record(s) — the trail has no meaning without it. The project is not affected. ' +
      'This cannot be undone.',
    danger: true,
    confirmLabel: 'Delete'
  }).then(function (ok) {
    if (!ok) return;
    Design.remove(row.id);
    ui.toast(row.id + ' deleted', 'success');
    EPAL.router.render();
  });
}

})();
