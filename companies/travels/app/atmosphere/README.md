# Epal Travels & Consultancy — ambient scene

This company's ambient background: **airport (app/atmosphere/travels-scene.{css,js})**. Self-contained
(no external assets, GPU-only), bound to this vertical via the `data-atmos`
attribute the kernel stamps on `#view`. Kept as scoped CSS per the owner's
Phase-4 ruling. See `platform/design-system/ATMOSPHERE-SCENES.md`.
