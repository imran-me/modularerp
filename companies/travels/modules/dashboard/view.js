/* ============================================================================
 * AUTO-GENERATED by tools/build/build-module.mjs — DO NOT EDIT THIS FILE.
 * Sources (edit these, then rebuild):
 *   frontend/template.html   — the markup
 *   frontend/dashboard.js   — the logic
 *   (no module stylesheet)
 * Rebuild:  node tools/build/build-module.mjs companies/travels/modules/dashboard
 * ==========================================================================*/
(function () {
  'use strict';
  var TEMPLATE_HTML = "<!-- ============================================================================\n  TRAVELS · DASHBOARD · MARKUP\n  ----------------------------------------------------------------------------\n  The screen's HTML, separated from its logic (frontend/dashboard.js). Cloned +\n  filled at runtime via [data-tpl] / [data-slot] hooks.\n\n  STYLING = the house design system verbatim. Tailwind tw- utilities appear ONLY\n  where the legacy view used a static inline style, one property each (zero drift):\n    tw-cursor-pointer == style=\"cursor:pointer\"   (click-through rows)\n    tw-ml-auto        == style=\"margin-left:auto\"  (card-head right cluster)\n    tw-h-[260px]      == style=\"height:260px\"       (chart containers)\n    tw-relative       == style=\"position:relative\"\n    tw-w-[90px]       == style=\"width:90px\"          (destination progress track)\n    tw-text-[20px]/[18px] == style=\"font-size:20/18px\" (flag glyphs)\n  The DYNAMIC progress-bar width (data-driven %) stays an inline style, set in\n  logic — a runtime value, not expressible as a static utility.\n\n  Each fragment is ONE line, no inter-tag whitespace — a clone is byte-for-byte\n  the DOM the old ui.el() calls produced.\n  ============================================================================ -->\n\n<!-- page shell ------------------------------------------------------------->\n<template data-tpl=\"page\"><div class=\"page\"></div></template>\n\n<!-- KPI card (drill variant + trend foot added in logic) ------------------->\n<template data-tpl=\"kpi\"><div class=\"kpi-card\"><div class=\"kpi-top\"><span class=\"kpi-label\" data-slot=\"label\"></span><span class=\"kpi-ico\" data-slot=\"ico\"></span></div><div class=\"kpi-value\" data-slot=\"value\"></div><div class=\"kpi-foot\" data-slot=\"foot\"></div></div></template>\n<template data-tpl=\"kpi-grid\"><div class=\"kpi-grid stagger\"></div></template>\n\n<!-- generic section label + two-column row shell --------------------------->\n<template data-tpl=\"section-label\"><div class=\"section-label\"></div></template>\n<template data-tpl=\"two-col\"><div class=\"two-col\"></div></template>\n\n<!-- Action Center: card + one alert row (chevron is static) ---------------->\n<template data-tpl=\"ac-card\"><div class=\"card\"><div class=\"card-body\" data-slot=\"rows\"></div></div></template>\n<template data-tpl=\"ac-row\"><div class=\"data-row tw-cursor-pointer\"><span class=\"notif-ico\" data-slot=\"ico\"></span><div class=\"flex-1\"><span class=\"strong\" data-slot=\"n\"></span><span class=\"text-dim\" data-slot=\"text\"></span></div><span class=\"text-mute\"><i class=\"bi bi-chevron-right\"></i></span></div></template>\n\n<!-- Payment Schedule widget: card (custom head) + stat row + row + empty --->\n<template data-tpl=\"sched-card\"><div class=\"card mb-3\"><div class=\"card-head\"><h3><i class=\"bi bi-calendar-week\"></i> Payment Schedule</h3><div class=\"flex gap-1 tw-ml-auto\"><button class=\"btn btn-sm btn-ghost\" data-slot=\"print\"><i class=\"bi bi-printer\"></i> Print Sheet</button><button class=\"btn btn-sm btn-primary\" data-slot=\"add\"><i class=\"bi bi-plus-lg\"></i> Add Schedule</button></div></div><div class=\"card-body\" data-slot=\"body\"></div></div></template>\n<template data-tpl=\"sched-stats\"><div class=\"stat-row mb-2\"><div class=\"stat\"><div class=\"stat-label\">Next 7 days</div><div class=\"stat-value\" data-slot=\"s7\"></div></div><div class=\"stat\"><div class=\"stat-label\">Next 15 days</div><div class=\"stat-value\" data-slot=\"s15\"></div></div></div></template>\n<template data-tpl=\"sched-empty\"><div class=\"text-mute sm\">Nothing due in the next 15 days.</div></template>\n<template data-tpl=\"sched-row\"><div class=\"data-row tw-cursor-pointer\"><span class=\"notif-ico\" data-slot=\"ico\"></span><div class=\"flex-1\"><div class=\"fw-600 sm\" data-slot=\"party\"></div><div class=\"text-mute xs\" data-slot=\"desc\"></div></div><div class=\"strong\" data-slot=\"amount\"></div></div></template>\n\n<!-- Flight Deadlines widget: card (head with static board link) + shared row -->\n<template data-tpl=\"flight-card\"><div class=\"card mb-3\"><div class=\"card-head\"><h3><i class=\"bi bi-broadcast\"></i> Flight Deadlines</h3><a class=\"btn btn-sm btn-ghost tw-ml-auto\" href=\"#/travels/contract-flight/day-board\"><i class=\"bi bi-calendar-day\"></i> Departures Board</a></div><div class=\"card-body\" data-slot=\"body\"></div></div></template>\n<template data-tpl=\"flight-row\"><div class=\"data-row tw-cursor-pointer\"><span class=\"notif-ico\" data-slot=\"ico\"></span><div class=\"flex-1\"><div class=\"fw-600 sm\" data-slot=\"title\"></div><div class=\"text-mute xs\" data-slot=\"sub\"></div></div><span class=\"badge\" data-slot=\"badge\"></span></div></template>\n\n<!-- trend + visa-funnel cards (canvas containers are static) --------------->\n<template data-tpl=\"trend-card\"><div class=\"card\"><div class=\"card-head\"><h3><i class=\"bi bi-activity\"></i> Sales &amp; Profit Trend</h3><span class=\"card-sub\">monthly</span></div><div class=\"card-body\"><div class=\"tw-h-[260px] tw-relative\"><canvas id=\"tv-trend\"></canvas></div></div></div></template>\n<template data-tpl=\"stages-card\"><div class=\"card\"><div class=\"card-head\"><h3><i class=\"bi bi-diagram-2\"></i> Visa Pipeline</h3></div><div class=\"card-body\"><div class=\"tw-h-[260px] tw-relative\"><canvas id=\"tv-stages\"></canvas></div></div></div></template>\n\n<!-- Top destinations card + row (progress-bar width set in logic) ---------->\n<template data-tpl=\"dest-card\"><div class=\"card\"><div class=\"card-head\"><h3><i class=\"bi bi-geo-alt-fill\"></i> Top Destinations</h3></div><div class=\"card-body\"><div class=\"data-list\" data-slot=\"rows\"></div></div></div></template>\n<template data-tpl=\"dest-row\"><div class=\"data-row\"><span class=\"tw-text-[20px]\" data-slot=\"flag\"></span><div class=\"flex-1 fw-600\" data-slot=\"country\"></div><div class=\"progress tw-w-[90px]\"><div class=\"progress-bar\" data-slot=\"bar\"></div></div><span class=\"badge badge-accent\" data-slot=\"apps\"></span></div></template>\n\n<!-- Recent applications card + row ---------------------------------------->\n<template data-tpl=\"recent-card\"><div class=\"card\"><div class=\"card-head\"><h3><i class=\"bi bi-clock-history\"></i> Recent Applications</h3><a class=\"link-btn\" href=\"#/travels/visa-processing/application-board\">View board</a></div><div class=\"card-body\"><div class=\"data-list\" data-slot=\"rows\"></div></div></div></template>\n<template data-tpl=\"recent-row\"><div class=\"data-row tw-cursor-pointer\"><span class=\"tw-text-[18px]\" data-slot=\"flag\"></span><div class=\"flex-1\"><div class=\"fw-600 sm\" data-slot=\"applicant\"></div><div class=\"text-mute xs\" data-slot=\"sub\"></div></div><span data-slot=\"badge\"></span></div></template>\n";
  var MODULE_CSS = null;
  if (MODULE_CSS && !document.querySelector('style[data-module-style="travels/dashboard"]')) {
    var st = document.createElement('style');
    st.setAttribute('data-module-style', 'travels/dashboard');
    st.textContent = MODULE_CSS;
    document.head.appendChild(st);
  }
  /* ---- frontend/dashboard.js ---------------------------------------- */
/* ============================================================================
 * TRAVELS · DASHBOARD · LOGIC
 * ----------------------------------------------------------------------------
 * Behaviour only — markup lives in frontend/template.html and is handed to this
 * file (by tools/build/build-module.mjs) as the string TEMPLATE_HTML. This file
 * is NOT an IIFE and has no 'use strict' of its own: the build wraps it.
 *
 * The company-level command view for Epal Travels — mirrors the Group Command
 * Center pattern scoped to one concern: money cockpit (from the double-entry
 * ledger, the single source of truth), Action Center, Payment Schedule + Flight
 * Deadlines widgets, sales/profit trend, visa funnel, top destinations, recent
 * applications. Charts via EPAL.charts; schedule add/print via EPAL.formModal.
 * ========================================================================== */

var EPAL = window.EPAL, ui = EPAL.ui, el = ui.el, db = EPAL.db, charts = EPAL.charts;

/* ---- template plumbing: clone a fragment, address its fill-slots ---------- */
var TPL = document.createElement('div');
TPL.innerHTML = TEMPLATE_HTML;
function frag(name) {
  var t = TPL.querySelector('template[data-tpl="' + name + '"]');
  return t.content.firstElementChild.cloneNode(true);
}
function slot(root, name) { return root.querySelector('[data-slot="' + name + '"]'); }

EPAL.view('travels/dashboard', {
  render: function (ctx) {
    var f = db.finance('travels', 12), s = db.series('travels');
    var apps = db.visaApps(), cats = db.visaCats();
    var page = frag('page');

    page.appendChild(EPAL.pageHead({
      eyebrow:'Epal Travels & Consultancy', icon:'airplane-fill', title:'Travels Dashboard',
      sub:'Air ticketing, visa processing and consultancy — live operational health.',
      actions: [
        el('button.btn.btn-ghost', { html: ui.icon('passport') + ' Visa Board', onclick: function(){ EPAL.router.navigate('travels/visa-processing/application-board'); } }),
        el('button.btn.btn-primary', { html: ui.icon('plus-lg') + ' New Visa Application', onclick: function(){ EPAL.router.navigate('travels/visa-processing/new-application'); } })
      ]
    }));

    // KPIs — the money cockpit. EVERY box is a door (checklist 05): office cash,
    // income, expense and the net result each open their detail screen.
    var visaRevenue = apps.reduce(function (a, x){ return a + (x.sale||0); }, 0);
    var visaProfit = apps.reduce(function (a, x){ return a + ((x.sale||0) - (x.cost||0)); }, 0);
    var approved = apps.filter(function (x){ return x.stage==='Approved'; }).length;
    var pending = apps.filter(function (x){ return ['New','Documents','Submitted','Under Process'].indexOf(x.stage)>=0; }).length;
    // ONE SOURCE OF TRUTH (audit fix): the headline money tiles read the
    // double-entry LEDGER — the same book Accounts, Ledgers and the Group
    // consolidation read — never the operational financials series (which
    // stays for trends only). Before this, dashboard vs books diverged 7-21%.
    var officeCash = 0, glIncome = 0, glExpense = 0;
    try {
      officeCash = Math.round(EPAL.ledger.balance('1000',{companyId:'travels'}) + EPAL.ledger.balance('1010',{companyId:'travels'}));
      EPAL.ledger.accounts().forEach(function (a) {
        if (a.type === 'income') glIncome += EPAL.ledger.balance(a.code, { companyId: 'travels' });
        else if (a.type === 'expense') glExpense += EPAL.ledger.balance(a.code, { companyId: 'travels' });
      });
    } catch (x) {}
    glIncome = Math.round(glIncome); glExpense = Math.round(glExpense);
    var netResult = glIncome - glExpense;
    var marginPct = glIncome ? (netResult / glIncome * 100) : 0;
    var grid = frag('kpi-grid');
    grid.appendChild(kpi('Office Cash & Bank', ui.money(officeCash,{compact:true}), 'bank', officeCash>=0?'up':'down', 'cash book', 'travels/accounts/cashbook'));
    grid.appendChild(kpi('Income', ui.money(glIncome,{compact:true}), 'graph-up-arrow', 'up', 'per the books', 'travels/accounts/income'));
    grid.appendChild(kpi('Expense', ui.money(glExpense,{compact:true}), 'wallet2', 'flat', 'per the books', 'travels/accounts/expenses'));
    grid.appendChild(kpi(netResult>=0?'Net Profit':'Net Loss', ui.money(Math.abs(netResult),{compact:true}), 'cash-stack', netResult>=0?'up':'down', ui.pct(marginPct)+' margin', 'travels/accounts'));
    grid.appendChild(kpi('Visa Pipeline', String(pending), 'passport', 'flat', pending+' in process', 'travels/visa-processing/application-board'));
    grid.appendChild(kpi('Visa Sales Value', ui.money(visaRevenue,{compact:true}), 'cash-coin', 'up', ui.money(visaProfit,{compact:true})+' profit', 'travels/visa-processing/manage-sales'));
    page.appendChild(grid);

    // --- Action Center: what needs attention today (each row navigates) ---
    var tickets = db.col ? db.col('airTickets') : [];
    var ttls    = db.col ? db.col('air_ttl') : [];
    var customers = db.customers ? db.customers('travels') : [];
    var now = Date.now();
    function daysLeft(d){ return d ? Math.round((new Date(d).getTime()-now)/86400000) : null; }
    function monthsLeft(d){ return d ? Math.round((new Date(d).getTime()-now)/(86400000*30.4)) : null; }
    var held = tickets.filter(function(t){ return t.status==='Hold'; });
    var ttlDue = ttls.filter(function(r){ var dl=daysLeft(r.ttl||r.deadline||r.due); return dl!=null && dl<=3 && r.status!=='Ticketed'; });
    var visaOverdue = apps.filter(function(a){ if(a.stage!=='Under Process')return false; var cat=cats.filter(function(c){return c.id===a.catId;})[0]||{days:14}; return new Date(new Date(a.created).getTime()+cat.days*86400000) < new Date(); });
    var pxSoon = customers.filter(function(c,i){ var exp=c.passportExpiry; if(!exp){ var d=new Date(2026,6,15); d.setMonth(d.getMonth()+(((i*7)%16)-2)); exp=d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-15'; } var m=monthsLeft(exp); return m!=null && m>=0 && m<=6; });
    var alerts = [
      held.length      ? { icon:'pause-circle-fill', tone:'warning', n:held.length,        text:'held tickets awaiting issue',            route:'travels/air-ticketing/manage-sales', match: held[0].passenger || held[0].pnr } : null,
      ttlDue.length    ? { icon:'alarm-fill',        tone:'error',   n:ttlDue.length,       text:'ticketing deadlines within 3 days',      route:'travels/air-ticketing/ttl', match: ttlDue[0].pnr || ttlDue[0].passenger || ttlDue[0].id } : null,
      visaOverdue.length ? { icon:'hourglass-bottom', tone:'error',  n:visaOverdue.length,  text:'visa decisions overdue',                 route:'travels/visa-processing/embassy-tracking', match: visaOverdue[0].applicant } : null,
      pxSoon.length    ? { icon:'person-vcard',      tone:'warning', n:pxSoon.length,       text:'customer passports expiring ≤6 months',  route:'travels/vendor-agent/customers', match: pxSoon[0].name } : null
    ].filter(Boolean);
    if (alerts.length) {
      var acLabel = frag('section-label'); acLabel.textContent = 'Action Center — needs attention'; page.appendChild(acLabel);
      var acCard = frag('ac-card'); var acBody = slot(acCard, 'rows');
      alerts.forEach(function (a) {
        var row = frag('ac-row');
        var ico = slot(row, 'ico'); ico.classList.add('notif-' + a.tone); ico.innerHTML = ui.icon(a.icon);
        slot(row, 'n').textContent = a.n + ' ';
        slot(row, 'text').textContent = a.text;
        row.addEventListener('click', (function(rt, mt){ return function(){ jumpTo(rt, mt); }; })(a.route, a.match));
        acBody.appendChild(row);
      });
      page.appendChild(acCard);
    }

    // --- Payment Schedule widget (checklist 05/06): upcoming 7 & 15 days, with
    // Add Schedule + a custom Print Sheet, directly from the dashboard. --------
    (function () {
      var scheds = (db.col ? db.col('acc_schedules') : []).filter(function (s) { return s.companyId === 'travels' && s.status !== 'Paid'; });
      function dTo(s) { var d = new Date(s.due); return isNaN(d) ? 9999 : Math.round((d.getTime() - now) / 86400000); }
      var in7 = scheds.filter(function (s) { var d = dTo(s); return d >= 0 && d <= 7; }).sort(function (a, b) { return a.due < b.due ? -1 : 1; });
      var in15 = scheds.filter(function (s) { var d = dTo(s); return d >= 0 && d <= 15; }).sort(function (a, b) { return a.due < b.due ? -1 : 1; });
      var sum7 = in7.reduce(function (a, s) { return a + (+s.amount || 0); }, 0);
      var sum15 = in15.reduce(function (a, s) { return a + (+s.amount || 0); }, 0);
      function printSheet() {
        EPAL.formModal({ title: 'Print Schedule Sheet', icon: 'printer', size: 'sm', record: { days: '15' },
          fields: [{ key: 'days', label: 'Horizon (days)', type: 'select', options: ['7', '15', '30', '60'], default: '15' }],
          saveLabel: 'Print',
          onSave: function (v) {
            var n = +v.days, list = scheds.filter(function (s) { var d = dTo(s); return d >= -365 && d <= n; }).sort(function (a, b) { return a.due < b.due ? -1 : 1; });
            var rows = list.map(function (s) { return '<tr><td>' + ui.escapeHtml(s.party || '') + '</td><td>' + ui.escapeHtml(s.kind || '') + '</td><td>' + ui.date(s.due) + '</td><td>' + ui.escapeHtml(s.status || '') + '</td><td style="text-align:right">' + ui.money(s.amount) + '</td></tr>'; }).join('');
            var tot = list.reduce(function (a, s) { return a + (+s.amount || 0); }, 0);
            ui.printDoc({ title: 'Payment Schedule — next ' + n + ' days', subtitle: 'Epal Travels & Consultancy · Accounts', meta: list.length + ' schedules · total ' + ui.money(tot), footer: 'System-generated schedule sheet',
              bodyHtml: '<table><tr><th>Party</th><th>Kind</th><th>Due</th><th>Status</th><th>Amount</th></tr>' + rows + '<tr><th colspan="4">Total</th><th style="text-align:right">' + ui.money(tot) + '</th></tr></table>' });
            return true;
          } });
      }
      function addSchedule() {
        EPAL.formModal({ title: 'Add Payment Schedule', icon: 'calendar2-plus', size: 'md', record: { kind: 'Payable', status: 'Pending' },
          fields: [
            { key: 'party', label: 'Party', type: 'text', required: true, placeholder: 'Vendor / customer / staff' },
            { key: 'kind', label: 'Kind', type: 'select', options: ['Payable', 'Receivable'], default: 'Payable' },
            { key: 'amount', label: 'Amount (৳)', type: 'money', required: true, min: 1 },
            { key: 'due', label: 'Due date', type: 'date', required: true },
            { key: 'desc', label: 'Note', type: 'textarea', col2: true }
          ],
          saveLabel: 'Add Schedule',
          onSave: function (v) {
            db.save('acc_schedules', { id: 'SCH-' + ui.uid('').slice(-5).toUpperCase(), companyId: 'travels', party: v.party, kind: v.kind, amount: +v.amount || 0, due: v.due, status: 'Pending', desc: v.desc || '' });
            ui.toast('Schedule added', 'success'); EPAL.router.render(); return true;
          } });
      }
      var card = frag('sched-card');
      slot(card, 'print').addEventListener('click', printSheet);
      slot(card, 'add').addEventListener('click', addSchedule);
      var bodyEl = slot(card, 'body');
      var stats = frag('sched-stats');
      slot(stats, 's7').textContent = in7.length + ' · ' + ui.money(sum7, { compact: true });
      slot(stats, 's15').textContent = in15.length + ' · ' + ui.money(sum15, { compact: true });
      bodyEl.appendChild(stats);
      if (!in15.length) bodyEl.appendChild(frag('sched-empty'));
      in15.slice(0, 6).forEach(function (s) {
        var d = dTo(s);
        var row = frag('sched-row');
        var ico = slot(row, 'ico'); ico.classList.add('notif-' + (d <= 3 ? 'error' : 'warning')); ico.innerHTML = ui.icon(s.kind === 'Payable' ? 'arrow-up-right-circle' : 'arrow-down-left-circle');
        slot(row, 'party').textContent = s.party || '—';
        slot(row, 'desc').textContent = s.kind + ' · due ' + ui.date(s.due) + (d === 0 ? ' (today)' : d > 0 ? ' (in ' + d + 'd)' : '');
        slot(row, 'amount').textContent = ui.money(s.amount);
        row.addEventListener('click', function () { jumpTo('travels/accounts/schedules', s.party); });
        bodyEl.appendChild(row);
      });
      page.appendChild(card);
    })();

    // --- FLIGHT DEADLINES widget — the production LIVE BULLETIN list:
    // contract-flight departures inside 4 days + urgent held-PNR ticketing
    // deadlines (48h). Click-through to the Departures Board / TTL queue.
    (function () {
      function locDay(off) { var d = new Date(); d.setDate(d.getDate() + (off || 0)); return d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0') + '-' + String(d.getDate()).padStart(2, '0'); }
      var todayS = locDay(0), horizon = locDay(4);
      var deps = (db.col ? db.col('tv_contract_flights') : []).filter(function (f) {
        var d = String(f.depDate || '').slice(0, 10);
        return f.status !== 'Departed' && d >= todayS && d <= horizon;
      }).sort(function (a, b) { return String(a.depDate) < String(b.depDate) ? -1 : 1; });
      var holds = (db.col ? db.col('air_ttl') : []).filter(function (r) {
        if (r.status !== 'Hold' || !r.ttl) return false;
        var h = (new Date(r.ttl) - Date.now()) / 3600000; return h > -24 && h <= 48;
      }).sort(function (a, b) { return new Date(a.ttl) - new Date(b.ttl); });
      if (!deps.length && !holds.length) return;
      var card = frag('flight-card'); var bodyF = slot(card, 'body');
      deps.slice(0, 4).forEach(function (f) {
        var days = Math.round((new Date(String(f.depDate).slice(0, 10) + 'T00:00:00') - new Date(todayS + 'T00:00:00')) / 86400000);
        var row = frag('flight-row');
        var ico = slot(row, 'ico'); ico.classList.add('notif-' + (days <= 1 ? 'error' : 'warning')); ico.innerHTML = ui.icon('airplane');
        slot(row, 'title').textContent = f.airline + ' ' + f.flightNo + ' · ' + f.route;
        slot(row, 'sub').textContent = 'departs ' + ui.date(f.depDate) + ' · ' + (days === 0 ? 'TODAY' : 'IN ' + days + ' DAY' + (days === 1 ? '' : 'S')) + ' · ' + (f.sold || 0) + '/' + (f.seats || 0) + ' sold';
        var badge = slot(row, 'badge'); badge.classList.add(days <= 1 ? 'badge-bad' : 'badge-warn'); badge.textContent = 'FLIGHT DEADLINE';
        row.addEventListener('click', function () { EPAL.router.navigate('travels/contract-flight/day-board'); });
        bodyF.appendChild(row);
      });
      holds.slice(0, 3).forEach(function (r) {
        var h = Math.round((new Date(r.ttl) - Date.now()) / 3600000);
        var row = frag('flight-row');
        var ico = slot(row, 'ico'); ico.classList.add('notif-' + (h <= 6 ? 'error' : 'warning')); ico.innerHTML = ui.icon('hourglass-split');
        slot(row, 'title').textContent = r.airline + ' · ' + r.route + ' · PNR ' + r.pnr;
        slot(row, 'sub').textContent = 'ticketing deadline ' + ui.date(String(r.ttl).slice(0, 10)) + ' ' + String(r.ttl).slice(11, 16) + ' · ' + (h < 0 ? 'OVERDUE' : 'in ' + h + 'h');
        var badge = slot(row, 'badge'); badge.classList.add(h <= 6 ? 'badge-bad' : 'badge-warn'); badge.textContent = 'TICKET TTL';
        row.addEventListener('click', function () { EPAL.router.navigate('travels/air-ticketing/ttl'); });
        bodyF.appendChild(row);
      });
      page.appendChild(card);
    })();

    // --- PRODUCT P&L (owner 2026-07-22): "cost per sell, margins … per product,
    // shown on the Travels Dashboard". Contribution view straight from the ONE
    // ledger — Revenue − direct COGS per product (Air / Visa / Contract), with
    // the count-driven cost-per-sale; the company opex + net close the block.
    // Reads EPAL.ledger.pnlByProduct + pnl, so it can't drift from the books. ---
    (function () {
      var prods = [], P = { revenue: 0, cogs: 0, gross: 0, expenses: 0, net: 0 };
      try { prods = EPAL.ledger.pnlByProduct('travels') || []; P = EPAL.ledger.pnl('travels') || P; } catch (e) {}
      function st(label, val, cls) {
        return el('div.stat', null, [ el('div.stat-label', { text: label }),
          el('div.stat-value.num' + (cls ? '.' + cls : ''), { text: val }) ]);
      }
      var body = el('div.card-body');
      if (!prods.length) {
        body.appendChild(el('p.text-mute.sm', { html: ui.icon('inbox') + ' No product sales posted to the books yet — issue a ticket, visa or contract sale and its revenue, cost and margin appear here.' }));
      } else {
        var routeOf = { 'Air Ticket': 'travels/air-ticketing/manage-sales', 'Visa': 'travels/visa-processing/manage-sales', 'Contract': 'travels/contract-flight', 'EMD/Ancillary': 'travels/air-ticketing/manage-sales' };
        var tb = el('tbody');
        prods.forEach(function (p) {
          var route = routeOf[p.product];
          var tr = el('tr' + (route ? '.drill' : ''), null, [
            el('td', { html: '<span class="strong">' + ui.escapeHtml(p.product) + '</span>' }),
            el('td.num', { text: String(p.count) }),
            el('td.num', { text: ui.money(p.revenue) }),
            el('td.num.text-mute', { text: ui.money(p.cogs) }),
            el('td.num.' + (p.gross >= 0 ? 'text-good' : 'text-bad'), { text: ui.money(p.gross) }),
            el('td.num', { html: '<span class="badge badge-' + (p.margin >= 0 ? 'good' : 'bad') + '">' + ui.pct(p.margin) + '</span>' }),
            el('td.num.text-mute', { text: ui.money(p.perSaleCost) })
          ]);
          if (route) { tr.setAttribute('title', 'Open ' + p.product + ' sales'); tr.addEventListener('click', function () { EPAL.router.navigate(route); }); }
          tb.appendChild(tr);
        });
        var tbl = el('table.tbl', null, [
          el('thead', null, [ el('tr', null, [
            el('th', { text: 'Product' }), el('th.num', { text: 'Sales' }), el('th.num', { text: 'Revenue' }),
            el('th.num', { text: 'Direct Cost' }), el('th.num', { text: 'Gross Margin' }),
            el('th.num', { text: 'Margin %' }), el('th.num', { text: 'Cost / Sale' }) ]) ]),
          tb
        ]);
        body.appendChild(tbl);
        body.appendChild(el('div.stat-row.mt-3', null, [
          st('Revenue', ui.money(P.revenue)),
          st('COGS', ui.money(P.cogs)),
          st('Gross Margin', ui.money(P.gross), P.gross >= 0 ? 'text-good' : 'text-bad'),
          st('Operating Exp', ui.money(P.expenses)),
          st(P.net >= 0 ? 'Net Profit' : 'Net Loss', ui.money(Math.abs(P.net)), P.net >= 0 ? 'text-good' : 'text-bad')
        ]));
      }
      page.appendChild(el('div.card.mb-3', null, [
        el('div.card-head', null, [
          el('h3', { html: ui.icon('graph-up-arrow') + ' Product P&L — cost &amp; margin per sale' }),
          el('span.card-sub', { text: 'straight from the books · all products' })
        ]),
        body
      ]));
    })();

    // trend + visa stage funnel
    var row = frag('two-col');
    row.appendChild(frag('trend-card'));
    // visa stage distribution
    var stages = ['New','Documents','Submitted','Under Process','Approved','Rejected'];
    var stageCounts = stages.map(function (st){ return apps.filter(function(x){return x.stage===st;}).length; });
    row.appendChild(frag('stages-card'));
    page.appendChild(row);

    // top destinations + recent applications
    var row2 = frag('two-col');
    var byCountry = {};
    apps.forEach(function (a){ byCountry[a.country] = (byCountry[a.country]||0)+1; });
    var top = Object.keys(byCountry).map(function (k){ return { country:k, n:byCountry[k], flag:(cats.filter(function(c){return c.country===k;})[0]||{}).flag||'🌍' }; })
      .sort(function (a,b){ return b.n-a.n; }).slice(0,6);
    var destCard = frag('dest-card'); var destRows = slot(destCard, 'rows');
    top.forEach(function (d) {
      var r = frag('dest-row');
      slot(r, 'flag').textContent = d.flag;
      slot(r, 'country').textContent = d.country;
      slot(r, 'bar').style.width = (d.n / top[0].n * 100) + '%';
      slot(r, 'apps').textContent = d.n + ' apps';
      destRows.appendChild(r);
    });
    row2.appendChild(destCard);
    var recentCard = frag('recent-card'); var recentRows = slot(recentCard, 'rows');
    apps.slice(0, 6).forEach(function (a) {
      var r = frag('recent-row');
      slot(r, 'flag').textContent = a.flag;
      slot(r, 'applicant').textContent = a.applicant;
      slot(r, 'sub').textContent = a.country + ' · ' + a.visaType;
      slot(r, 'badge').replaceWith(stageBadge(a.stage));
      r.addEventListener('click', function () { EPAL.router.navigate('travels/visa-processing/application-board'); });
      recentRows.appendChild(r);
    });
    row2.appendChild(recentCard);
    page.appendChild(row2);

    ctx.mount.appendChild(page);

    requestAnimationFrame(function () {
      charts.area(ui.$('#tv-trend'), { labels:s.labels, datasets:[
        { label:'Revenue', data:s.revenue, color:'#2f6bff' },
        { label:'Profit', data:s.profit, color:'#23c17e' } ] });
      charts.bar(ui.$('#tv-stages'), { labels:stages, datasets:[{ label:'Applications', data:stageCounts,
        colors:['#8b93a7','#7b5cff','#2f6bff','#f4b740','#23c17e','#f0506e'] }], money:false });
    });
  }
});

/* one KPI card — drill variant (route) + optional trend foot */
function kpi(label, value, icon, dir, foot, route) {
  var n = frag('kpi');
  if (route) { n.classList.add('drill'); n.addEventListener('click', function () { EPAL.router.navigate(route); }); n.setAttribute('title', 'Open ' + label); }
  slot(n, 'label').textContent = label;
  slot(n, 'ico').innerHTML = '<i class="bi bi-' + icon + '"></i>';
  slot(n, 'value').textContent = value;
  if (foot) {
    var arrow = dir === 'up' ? ui.icon('arrow-up-right') : dir === 'down' ? ui.icon('arrow-down-right') : ui.icon('dash');
    slot(n, 'foot').appendChild(el('span.kpi-trend.' + (dir || 'flat'), { html: arrow + ' ' + foot }));
  }
  return n;
}
/* Navigate to a route, then land the user DIRECTLY on the matching row —
 * scrolled to centre and flashed — so nobody hunts through a table (checklist:
 * "click korle direct fixed position-e show korbe, scroll jeno na lage"). */
function jumpTo(route, match) {
  EPAL.router.navigate(route);
  if (!match) return;
  var tries = 0;
  (function seek() {
    if (++tries > 25) return;                      // give up quietly after ~2.5s
    var view = document.getElementById('view');
    var rows = view ? view.querySelectorAll('.tbl tbody tr, .data-row, .kb-card') : [];
    for (var i = 0; i < rows.length; i++) {
      if ((rows[i].textContent || '').indexOf(match) >= 0) {
        rows[i].scrollIntoView({ block: 'center', behavior: 'smooth' });
        rows[i].classList.add('row-flash');
        setTimeout((function (r) { return function () { r.classList.remove('row-flash'); }; })(rows[i]), 2600);
        return;
      }
    }
    setTimeout(seek, 100);
  })();
}
function stageBadge(stage) {
  var map = { 'Approved':'good','Rejected':'bad','Under Process':'warn','Submitted':'info','Documents':'','New':'' };
  return el('span.badge'+(map[stage]?'.badge-'+map[stage]:''), { text: stage });
}
EPAL.travelStageBadge = stageBadge;

})();
