# Epal Shop — ambient scene (not yet created)

Travels has a dusk airport and Woodart a build-yourself interior room. A
signature ambient scene for **Shop** is a future creative build
(e.g. a storefront / retail floor). Until then this
vertical shows the faint corner emblem from
`platform/design-system/css/atmosphere.css`. Drop a `shop-scene.{css,js}`
pair here and bind it on `data-atmos="shop"` — copy an existing scene's
mount block.
